describe('archwayClient', () => {
  describe('metadata', () => {
    describe('set', () => {
      it.todo('can set a contract metadata');
    });

    describe('get', () => {
      it.todo('can get a contract metadata');
    });
  });
});
